/** 
 * This is a button that shows a VowMaker below.
 */

import React, {useState} from "React"
 
const VowMakerButton = () => (
  <div className="VowMakerButton">
    {/*TODO*/}
    <VowMaker />
  </div>
)

/** 
 * In this box the user can set their bond,
 * upload their Vow as a file and input the
 * zeroDay, aka, the day at which Vow must be
 * completed.
 */

const VowMaker = () => (
  <div className="VowMaker">
    {/*TODO*/}
  </div>
)

export default VowMakerButton;
