/** 
 * Shows your usual footer below.
 * Links to:
 * - achiever's github
 * - kleros.io (Or whatever the current Arbitrator is)
 * - docs
 * - a donate icon that shows "Click to paste ETH address"
 * - total ETH in bonds.
 */

import React, {useState} from "React"

const Footer = () => (
  <div className="Footer">
    {/*TODO*/}
  </div>
)

export default Footer;
